// Referral sidebar links and promotional content are enabled.
export const REFERRALS_ENABLED = true;

// Google users enter the app directly without the extra country/currency setup screen.
export const GOOGLE_PROFILE_SETUP_ENABLED = false;
