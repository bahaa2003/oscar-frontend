// Referral sidebar links and promotional content are enabled.
export const REFERRALS_ENABLED = true;

// Google users must complete server-required country/currency setup before app access.
export const GOOGLE_PROFILE_SETUP_ENABLED = true;
